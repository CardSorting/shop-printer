/** Purchase intent TTL — cart is advisory, not truth. */
export const CART_INTENT_TTL_MS = 30 * 24 * 60 * 60 * 1000;

export const CART_DEFAULT_CURRENCY = 'USD';

/** Low-stock threshold for advisory availability display. */
export const CART_LOW_STOCK_THRESHOLD = 3;
